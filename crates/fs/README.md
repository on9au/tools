# fs

Filesystem-related utilities

## `reaper`

A windows-only tool to kill (or list) processes locking a file or directory. Useful for cleaning up build artifacts that are locked by a hung process.
